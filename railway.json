{
    "build": {
        "dockerfilePath": "Dockerfile"
    },
    "start": {
        "dockerfilePath": "Dockerfile",
        "command": "streamlit run app/main.py --server.port=$PORT --server.address=0.0.0.0"
    }
}